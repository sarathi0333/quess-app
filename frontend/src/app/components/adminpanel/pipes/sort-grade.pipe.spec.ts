import { SortGradePipe } from './sort-grade.pipe';

describe('SortGradePipe', () => {
  it('create an instance', () => {
    const pipe = new SortGradePipe();
    expect(pipe).toBeTruthy();
  });
});
